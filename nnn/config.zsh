# Default options
# d   detail mode
# H   show hidden files
# i   show current file information in info bar (may be slow)
# o   open files only on Enter key
# U show user and group names in status bar
export NNN_OPTS="dHioU"
