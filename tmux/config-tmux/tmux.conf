# -- General -------------------------------------------------------------------

set -g default-terminal "screen-256color"

# Enable 'extended-keys' if supported for Control + Shift bindings
%if #{>=:#{version},3.2}
set -g extended-keys on
%endif

set -s escape-time 10       # No delay for escapes
set -sg repeat-time 600     # more time for repeat commands like window resize
set -s focus-events on

set -g prefix C-a           # Remap prefix to Control + a
unbind C-b

bind a send-prefix          # Map Control + a then a to regular Control + a

set -q -g status-utf8 on
setw -q -g utf8 on

set -g history-limit 10000  # increase history size


# Use Emacs bindings on the command line
set -g status-keys emacs

# Use Vim bindings when in copy mode
set -g mode-keys vi

# Setup 'v' to begin selection as in Vim
bind -Tcopy-mode-vi 'v' send -X begin-selection

# Setup 'y' to copy selection as in Vim
bind -Tcopy-mode-vi 'y' send -X copy-selection-and-cancel

set -s set-clipboard off

# Popup to today's note
bind C-t display-popup -w 80% -h 80% -E nvim -c "cd ~/Sync/notes/personal" -c "Obsidian today"

# reload config
bind r {
  # source-file -F "#{config_files}"
  source-file "~/.config/tmux/tmux.conf"
  display-message "tmux config reloaded"
}


# -- Display -------------------------------------------------------------------

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time

set -g status-interval 10     # redraw status line every 10 seconds

# Don't show activity on windows
set -g monitor-activity off
set -g visual-activity off

# Needed for displaying images
set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM


# -- Navigation ----------------------------------------------------------------

# Create session
bind C-c command-prompt -p "New session name:" 'new-session -s %%'

# Rename session
bind C-r command-prompt -I "#{session_name}" -p "Rename session:" 'rename-session %%'

# session navigation
bind Tab switch-client -l  # move to last session

# split current window horizontally
bind - split-window -v -c "#{pane_current_path}"
# split current window vertically
bind _ split-window -h -c "#{pane_current_path}"
bind \\ split-window -h -c "#{pane_current_path}"

bind > swap-pane -D  # swap current pane with the next one
bind < swap-pane -U  # swap current pane with the previous one

# Smart pane switching with awareness of vim splits
# See https://github.com/aserowy/tmux.nvim
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?\.?(view|n?vim?x?)(-wrapped)?(diff)?$'"

bind -n 'C-h' if "$is_vim" 'send C-h' { if -F '#{pane_at_left}'   '' 'select-pane -L' }
bind -n 'C-j' if "$is_vim" 'send C-j' { if -F '#{pane_at_bottom}' '' 'select-pane -D' }
bind -n 'C-k' if "$is_vim" 'send C-k' { if -F '#{pane_at_top}'    '' 'select-pane -U' }
bind -n 'C-l' if "$is_vim" 'send C-l' { if -F '#{pane_at_right}'  '' 'select-pane -R' }

# bind -T copy-mode-vi 'C-h' if -F '#{pane_at_left}' '' 'select-pane -L'
# bind -T copy-mode-vi 'C-j' if -F '#{pane_at_bottom}' '' 'select-pane -D'
# bind -T copy-mode-vi 'C-k' if -F '#{pane_at_top}' '' 'select-pane -U'
# bind -T copy-mode-vi 'C-l' if -F '#{pane_at_right}' '' 'select-pane -R'

# pane resizing
bind -r H resize-pane -L 2
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 2

# Toggle mouse mode (default on)
set -g mouse on
bind m {
  set -g mouse
  display "mouse: #{?mouse,on,off}"
}

# Quick window cycling
unbind ^A
bind ^A select-window -l

# Browse URLs in current window
bind u {
  capture-pane -J -S - -E - -b 'url-capture'
  split-window "tmux save-buffer -b 'url-capture' - | urlscan --compact --dedupe --single"
}

bind F {
  capture-pane -J -b 'fpp-capture'
  split-window "tmux save-buffer -b 'fpp-capture' - | fpp || true"
}

bind P choose-buffer    # choose which buffer to paste from


# -- Theme ---------------------------------------------------------------------

# Dracula Color Pallette
## Base Dracula PRO
%hidden black="#22212c"
%hidden red="#ff9580"
%hidden green="#8aff80"
%hidden yellow="#ffff80"
%hidden blue="#9580ff"
%hidden magenta="#ff80bf"
%hidden cyan="#80ffea"
%hidden white="#f8f8f2"
%hidden brightblack="#504c67"
%hidden brightred="#ffaa99"
%hidden brightgreen="#a2ff99"
%hidden brightyellow="#ffff99"
%hidden brightblue="#aa99ff"
%hidden brightmagenta="#ff99cc"
%hidden brightcyan="#99ffee"
%hidden brightwhite="#ffffff"
%hidden dimblack="#1b1a23"
%hidden dimred="#cc7766"
%hidden dimgreen="#6ecc66"
%hidden dimyellow="#cccc66"
%hidden dimblue="#7766cc"
%hidden dimmagenta="#cc6699"
%hidden dimcyan="#66ccbb"
%hidden dimwhite="#c6c6c2"
%hidden background="#22212c"
%hidden foreground="#f8f8f2"
%hidden comment="#7970a9"
%hidden selection="#454158"
%hidden cursor="#7970a9"
%hidden orange="#ffca80"


set -g status-left ' #S '
set -g status-right ' %Y-%m-%d %H:%M '
set -g status-style 'fg=#{white} bg=#{background}'

set -g clock-mode-style 12

set -g pane-border-indicators both
set -g pane-border-style 'fg=#{brightblack}'
set -g pane-active-border-style 'fg=#{blue}'

set -g window-status-style 'bg=default fg=default'
set -g window-status-current-style 'bg=#{magenta} fg=default'
set -g window-status-last-style 'bg=#{comment} fg=default'

set -g message-style 'bg=#{brightmagenta}'

set -g copy-mode-position-style 'bg=#{blue}'
set -g copy-mode-selection-style 'bg=#{selection}'

set -g window-status-format ' #{window_index} #{window_name}#{?window_zoomed_flag,  } '
set -g window-status-current-format '-#{window_index} #{window_name}#{?window_zoomed_flag,  }-'

set -g popup-border-lines rounded

# set -gw pane-border-format

# set -gw window-status-activity-style 'fg=#ff000'
# set -gw window-status-bell-style 'fg=#ff000'
# set -gw window-status-current-style 'fg=#ff000'
# set -gw window-status-last-style 'fg=#ff000'
# set -gw window-status-style 'fg=#ff000'
# set -gw cursor-style 'fg=#ff000'
# set -gw window-active-style 'fg=#ff000'
# set -gw window-style 'fg=#ff000'

# Available elements to style:
# menu-style style
# menu-selected-style style
# menu-border-style style
# message-command-style style
# message-style style
# prompt-cursor-style style
# prompt-command-cursor-style style
# status-left-style style
# status-right-style style
# status-style style
# clock-mode-style [12 | 24 | 12-with-seconds | 24-with-seconds]
# copy-mode-match-style style
# copy-mode-mark-style style
# copy-mode-current-match-style style
# copy-mode-position-style style
# copy-mode-selection-style style
# mode-style style
# pane-active-border-style style
# pane-border-style style
# popup-style style
# popup-border-style style
# pane-scrollbars-style style
# pane-status-current-style style
# pane-status-style style
# session-status-current-style style
# session-status-style style
# window-status-activity-style style
# window-status-bell-style style
# window-status-current-style style
# window-status-last-style style
# window-status-style style
# cursor-style style
# window-active-style style
# window-style style

